package com.aticatac.ui.lobbymenu.handlers;

import com.aticatac.lobby.utils.LobbyServer;
import javafx.animation.AnimationTimer;
import javafx.scene.canvas.GraphicsContext;

public class LMAnimation extends AnimationTimer {
    public LMAnimation(GraphicsContext gc, LobbyServer server) {

    }

    @Override
    public void handle(long now) {

    }
}
